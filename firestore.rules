rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Lock down all client-side direct access.
    // All read/write operations must go through Next.js API routes (Admin SDK).
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
